<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>OAuth2 Authentication Demo</h1>

    <% if (user) { %>
    <p>You're already logged in as <strong><%= user.email %></strong></p>
    <p><a href="/profile">View Profile</a></p>
    <% } else { %>
    <p>This demo shows OAuth2 authentication using Google.</p>

    <h2>Setup Required</h2>
    <p><strong>This demo requires Google OAuth2 credentials.</strong></p>
    <p>To use this demo:</p>
    <ol>
      <li>
        Go to
        <a href="https://console.cloud.google.com" target="_blank"
          >Google Cloud Console</a
        >
      </li>
      <li>Create OAuth 2.0 credentials</li>
      <li>
        Set redirect URI to:
        <code>http://localhost:3005/auth/google/callback</code>
      </li>
      <li>Add credentials to <code>.env</code> file</li>
    </ol>
    <p>See README.md for detailed instructions.</p>

    <hr />

    <p><a href="/auth/google">Sign in with Google</a></p>
    <% } %>
  </body>
</html>